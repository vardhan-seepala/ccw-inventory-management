{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.js|*.jsx|*.ts|*.tsx|*.vue|*.css|*.json|*.md) client/node_modules/.bin/prettier --write \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Running Prettier..."
          }
        ]
      }
    ]
  }
}
